################################################################################
# Common Secrets Template
#
# These are secrets that are the same across environments.
# Typically you won't need this file - use environment-specific secrets instead.
#
# For encrypted secrets, use SOPS:
#   sops -e secrets.yaml.example > secrets.yaml
################################################################################

# Cloudflare API credentials (if shared across environments)
# cloudflare:
#   apiToken: ""
#   zoneId: ""
